# MySQL Connection Settings (Railway)
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=123456
DB_NAME=bluetrack_db

# API Configuration
NODE_ENV=development
PORT=5000

# Note: For Railway deployment, Railway will set these environment variables automatically
# Just ensure DB_HOST, DB_PORT, DB_USER, DB_PASSWORD, and DB_NAME are configured in Railway dashboard
